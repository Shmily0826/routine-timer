# 发布与审核清单

小程序 appid：`wxbd0de427d588fa95`（见 `project.config.json`）。

## 一、发布流程

```
改代码 → npm run verify（本地全绿）
       → 双击 upload-experience.bat      # 传为「开发版」
       → mp.weixin.qq.com 后台把它「设为体验版」
       → 手机扫码真机验收（体验版二维码长期有效）
       → 后台「提交审核」→ 审核通过 → 「发布」
```

三个 bat 的分工：

| 脚本 | 产出 | 什么时候用 |
|---|---|---|
| `start-automation.bat` | 开 9420 自动化端口 | 我要跑 automator 套件时 |
| `preview-qrcode.bat` | 预览二维码（约 25 分钟有效） | 临时给朋友看一眼 |
| `upload-experience.bat` | 开发版（可长期作为体验版） | **真机验收、正式发布前** |

上传前脚本会自动跑 `npm run build:wechat`；版本号不指定时自动 patch +1（首次 `1.0.0`，记录在 `.workbuddy/last-upload.json`）。

## 二、提审前必须做的一件事：隐私保护指引

这是**最容易卡住**的一步，而且不是代码问题：微信要求每个小程序配置《用户隐私保护指引》，**未配置或生效中的版本提审会被直接打回**。

后台路径：设置 → 服务内容声明 → 用户隐私保护指引 → 更新。

本程序实际用到的接口：

| 接口 | 用途 | 是否需用户授权 |
|---|---|---|
| `wx.setStorageSync` / `getStorageSync` | 保存会话、Routine、历史 | 否（本地存储） |
| `wx.vibrateShort` | 阶段切换与倒计时震动 | 否 |
| `wx.createInnerAudioContext` | 播放 `assets/cue.wav` | 否 |
| `wx.setKeepScreenOn` | 计时中保持常亮 | 否 |

也就是说：**本程序不收集、不上传任何个人信息**，指引里勾选"不收集个人信息"即可。提交后通常需要 1~3 天生效，所以建议**在功能还没完工时就先把它提交了**。

## 三、审核材料清单

| 材料 | 建议 | 备注 |
|---|---|---|
| 名称 | 「分组练习计时器」或类似 | 名称全平台唯一，被占用就加后缀；别用极限词（最、第一）、别含品牌/地名 |
| 类目 | 工具 → 效率 | 工具类不需要额外资质；"运动 / 健身"类目审核更严，不建议 |
| 简介 | 120 字以内，说清"分组训练计时 + 保存方案 + 历史统计" | 别写"最好用"这类话 |
| 截图 | 5 张：首页设置 / 计时中 / 完成页 / 历史统计 / 深色模式 | 用体验版在真机上截（模拟器截图容易显得假）；建议 1080×1920 或微信要求的比例 |
| 测试账号 | 不需要 | 无登录、无服务端，所有数据在本地 |
| 服务地区 | 默认中国内地 | — |

## 四、提审前的自查（照着点一遍）

在体验版上跑，别在模拟器上跑：

- [ ] 计时准确：2 组 × 30 秒，跟手机自带秒表对一下
- [ ] 阶段切换提示音 + 震动（系统「设置 → 声音与触感 → 触感反馈」要是开的）
- [ ] 倒计时 3 秒的轻震动
- [ ] 暂停 / 继续 / 上一组 / 下一组
- [ ] **「再来一次」之后时钟继续走**（这里出过 bug，见 `test/timer_page.test.mjs`）
- [ ] 计时中切到微信聊天再回来，能续上；首页出现"发现未完成的训练"
- [ ] 点「放弃」后卡片消失且不再出现
- [ ] 保存 Routine → 编辑 → 复制 → 重命名 → 删除
- [ ] 完成一次练习后历史页多一条，累计统计数字对得上
- [ ] 深色模式（系统切深色）下所有文字可读
- [ ] 连续点「开始练习」不会开两个会话

自动化部分本地跑：`npm run verify`（需要先用 `start-automation.bat` 开 9420 端口，automator 套件才会真的跑起来）。

## 五、包体积

`miniprogram/` 约 85 KB（上限 2 MB），无需分包。

里面带着 `.ts` 源文件和 `.js.map`（各 7 个），只是让包变大一点点，不影响运行。想瘦身可在 `project.config.json` 加：

```json
"packOptions": {
  "ignore": [
    { "type": "file", "value": "*.ts" },
    { "type": "file", "value": "*.map" }
  ]
}
```

## 六、驳回常见原因（提前避坑）

- **隐私保护指引未配置 / 未生效** —— 见第二节
- **功能不完整 / 有 bug** —— 体验版上把第四节清单点一遍再提
- **类目与内容不符** —— 别挂"运动"类目
- **名称含极限词或侵权** —— 换名字
- 个人主体的小程序做不了需要资质的类目（电商、医疗、金融等），本程序不受影响。
