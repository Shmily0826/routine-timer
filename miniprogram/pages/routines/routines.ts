import {ROUTINES_KEY,SESSION_KEY,ACTIVE_ROUNDS_KEY,nextRoutineName} from '../../domain/storage';
Page({data:{routines:[] as any[]},onShow(){this.load()},load(){try{const x=wx.getStorageSync(ROUTINES_KEY);this.setData({routines:Array.isArray(x)?x.sort((a:any,b:any)=>(b.lastUsedAt||0)-(a.lastUsedAt||0)):[]})}catch{this.setData({routines:[]})}},save(){const rounds=wx.getStorageSync(ACTIVE_ROUNDS_KEY)||[];if(!rounds.length)return;const now=Date.now();wx.setStorageSync(ROUTINES_KEY,this.data.routines.concat({id:String(now),name:nextRoutineName(this.data.routines),rounds,createdAt:now,updatedAt:now}));this.load()},start(e:any){const list=this.data.routines.slice(),i=e.currentTarget.dataset.index;list[i].lastUsedAt=Date.now();wx.setStorageSync(ROUTINES_KEY,list);wx.setStorageSync(ACTIVE_ROUNDS_KEY,list[i].rounds);wx.removeStorageSync(SESSION_KEY);wx.navigateTo({url:'/pages/timer/timer'})},rename(e:any){const i=e.currentTarget.dataset.index;const cur=this.data.routines[i];if(!cur)return;wx.showModal({title:'重命名',editable:true,placeholderText:cur.name,success:(res:any)=>{if(!res.confirm)return;const name=String(res.content||'').trim();if(!name)return;const updated=this.data.routines.slice();updated[i]={...updated[i],name,updatedAt:Date.now()};wx.setStorageSync(ROUTINES_KEY,updated);this.load()}})},remove(e:any){wx.setStorageSync(ROUTINES_KEY,this.data.routines.filter((_:any,i:number)=>i!==e.currentTarget.dataset.index));this.load()},edit(e:any){const i=e.currentTarget.dataset.index;const r=this.data.routines[i];if(r)wx.navigateTo({url:'/pages/home/home?edit='+r.id})},duplicate(e:any){const list=this.data.routines.slice(),i=e.currentTarget.dataset.index;const src=list[i];if(!src)return;const now=Date.now();list.splice(i+1,0,{...src,id:String(now),name:src.name+'（副本）',createdAt:now,updatedAt:now,lastUsedAt:0});wx.setStorageSync(ROUTINES_KEY,list);this.load()}})
