// Ambient WeChat Miniprogram globals (`wx`, `Page`, `setInterval`, ...) for
// `tsc --noEmit` type checking of production code (pages + Timer Engine).
// The real types come from `miniprogram-api-typings`, which is auto-included
// via `typeRoots` in tsconfig.json (see tsconfig.json). This file is kept only
// as documentation of that wiring; it declares nothing itself.
